services:
  server:
    build:
      context: ${BUILD_CONTEXT:?Use build-instance.sh}
      dockerfile: packages/twenty-docker/twenty/Dockerfile
      target: twenty
      args:
        APP_VERSION: 0.0.0-hardened.${SOURCE_REVISION:?Set the audited source revision}
  worker:
    build:
      context: ${BUILD_CONTEXT:?Use build-instance.sh}
      dockerfile: packages/twenty-docker/twenty/Dockerfile
      target: twenty
      args:
        APP_VERSION: 0.0.0-hardened.${SOURCE_REVISION:?Set the audited source revision}
